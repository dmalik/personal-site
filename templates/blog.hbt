{{> header}}

<h2>{{title}}</h2>

<article>
  <ul>
    {{#each collections.lastArticles}}
      <li>
        <h3>{{this.title}} - {{formatDate this.date}}</h3>
        <div>{{{this.excerpt}}}</div>
      </li>
    {{/each}}
  </ul>
</article>

{{> footer}}